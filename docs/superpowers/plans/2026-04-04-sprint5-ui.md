# Sprint 5: UI Redesign — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Apply Style B (white/indigo, Inter font) consistently across all pages and extract shared components.
**Architecture:** CSS variables in index.css, shared components in frontend/src/components/, each page's inline styles replaced with class-based CSS importing index.css tokens.
**Tech Stack:** React 19, custom CSS, Inter (Google Fonts)

---

## Task 5.1: Add Inter font + global CSS tokens

- [ ] In `frontend/public/index.html`, add inside `<head>`:
  ```html
  <link
    href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap"
    rel="stylesheet"
  />
  ```
- [ ] Replace the entire contents of `frontend/src/index.css` with the following design tokens and global utility classes:

  ```css
  :root {
    --color-primary: #6366f1;
    --color-primary-dark: #4338ca;
    --color-primary-light: #e0e7ff;
    --color-success: #16a34a;
    --color-danger: #dc2626;
    --color-danger-light: #fee2e2;
    --color-text: #1e1b4b;
    --color-muted: #6b7280;
    --color-border: #e5e7eb;
    --color-bg: #f8faff;
    --font: "Inter", sans-serif;
    --radius: 10px;
    --shadow: 0 1px 4px rgba(99, 102, 241, 0.08);
    --shadow-md: 0 4px 12px rgba(99, 102, 241, 0.12);
  }
  * {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }
  body {
    font-family: var(--font);
    background: var(--color-bg);
    color: var(--color-text);
  }

  .btn-primary {
    background: var(--color-primary);
    color: #fff;
    border: none;
    border-radius: var(--radius);
    padding: 10px 20px;
    font-weight: 600;
    cursor: pointer;
    font-size: 14px;
    transition: background 0.2s;
  }
  .btn-primary:hover {
    background: var(--color-primary-dark);
  }
  .btn-primary:disabled {
    opacity: 0.6;
    cursor: not-allowed;
  }
  .btn-outline {
    background: #fff;
    color: var(--color-primary);
    border: 1.5px solid var(--color-primary);
    border-radius: var(--radius);
    padding: 9px 20px;
    font-weight: 600;
    cursor: pointer;
    font-size: 14px;
  }
  .btn-outline:hover {
    background: var(--color-primary-light);
  }
  .btn-danger {
    background: var(--color-danger);
    color: #fff;
    border: none;
    border-radius: var(--radius);
    padding: 10px 20px;
    font-weight: 600;
    cursor: pointer;
  }
  .btn-sm {
    background: var(--color-primary-light);
    color: var(--color-primary-dark);
    border: none;
    border-radius: 6px;
    padding: 4px 10px;
    font-size: 12px;
    cursor: pointer;
    font-weight: 600;
    margin-right: 4px;
  }
  .btn-danger-sm {
    background: var(--color-danger-light);
    color: var(--color-danger);
    border: none;
    border-radius: 6px;
    padding: 4px 10px;
    font-size: 12px;
    cursor: pointer;
    font-weight: 600;
  }
  .skill-tag {
    display: inline-block;
    padding: 2px 10px;
    border-radius: 12px;
    font-size: 12px;
    font-weight: 500;
    margin: 2px;
    background: var(--color-primary-light);
    color: var(--color-primary-dark);
  }
  .skill-tag.matched {
    background: var(--color-primary-light);
    color: var(--color-primary-dark);
  }
  .skill-tag.missing {
    background: var(--color-danger-light);
    color: var(--color-danger);
  }
  .skill-tag.preferred {
    background: #f0fdf4;
    color: #065f46;
  }
  .score-badge {
    display: inline-block;
    padding: 3px 10px;
    border-radius: 12px;
    color: #fff;
    font-weight: 700;
    font-size: 13px;
  }
  .status-chip {
    display: inline-block;
    padding: 2px 10px;
    border-radius: 12px;
    font-size: 12px;
    font-weight: 600;
    color: #fff;
    background: var(--color-muted);
  }
  .status-chip.status-shortlisted {
    background: var(--color-success);
  }
  .status-chip.status-pending {
    background: var(--color-primary);
  }
  .modal-overlay {
    position: fixed;
    inset: 0;
    background: rgba(0, 0, 0, 0.4);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 1000;
  }
  .modal {
    background: #fff;
    border-radius: var(--radius);
    padding: 32px;
    max-width: 600px;
    width: 90%;
    max-height: 85vh;
    overflow-y: auto;
    position: relative;
    box-shadow: var(--shadow-md);
  }
  .modal-close {
    position: absolute;
    top: 16px;
    right: 16px;
    background: none;
    border: none;
    font-size: 18px;
    cursor: pointer;
    color: var(--color-muted);
  }
  .loading {
    text-align: center;
    padding: 60px;
    color: var(--color-muted);
  }
  .empty-state {
    text-align: center;
    padding: 60px;
    color: var(--color-muted);
  }
  .error-banner {
    background: var(--color-danger-light);
    color: var(--color-danger);
    padding: 12px 16px;
    border-radius: var(--radius);
    margin-bottom: 16px;
  }
  .info-banner {
    background: var(--color-primary-light);
    color: var(--color-primary-dark);
    padding: 12px 16px;
    border-radius: var(--radius);
    margin-bottom: 16px;
  }
  .applied-badge {
    display: inline-block;
    background: #f0fdf4;
    color: var(--color-success);
    font-size: 12px;
    font-weight: 600;
    padding: 2px 8px;
    border-radius: 8px;
    margin-top: 4px;
  }
  ```

- [ ] Delete `frontend/src/App.css` (all its rules are now superseded by index.css).
- [ ] Run `cd frontend && npm start` and verify the app loads with the Inter font applied.

---

## Task 5.2: Create Navbar component

- [ ] Create the directory `frontend/src/components/` if it does not already exist.
- [ ] Create `frontend/src/components/Navbar.jsx` with the following content:

  ```jsx
  import React from "react";
  import { useNavigate, useLocation } from "react-router-dom";
  import "./Navbar.css";

  export default function Navbar({ user, onLogout, unreadMessages = 0 }) {
    const navigate = useNavigate();
    const location = useLocation();
    const isActive = (path) => location.pathname === path;

    const employeeLinks = [
      { path: "/jobs", label: "Find Jobs" },
      { path: "/applications", label: "My Applications" },
      { path: "/messages", label: "Messages", badge: unreadMessages },
      { path: "/profile", label: "Profile" },
    ];

    const employerLinks = [
      { path: "/dashboard", label: "Dashboard" },
      { path: "/my-jobs", label: "My Jobs" },
      { path: "/create-job", label: "Post a Job" },
      { path: "/messages", label: "Messages", badge: unreadMessages },
      { path: "/profile", label: "Profile" },
    ];

    const links = user?.role === "employer" ? employerLinks : employeeLinks;
    const initials = user?.name
      ? user.name
          .split(" ")
          .map((w) => w[0])
          .join("")
          .slice(0, 2)
          .toUpperCase()
      : "?";

    return (
      <nav className="navbar">
        <div className="navbar-brand" onClick={() => navigate("/jobs")}>
          IJM
        </div>
        <div className="navbar-links">
          {links.map((link) => (
            <button
              key={link.path}
              className={`nav-link ${isActive(link.path) ? "active" : ""}`}
              onClick={() => navigate(link.path)}
            >
              {link.label}
              {link.badge > 0 && (
                <span className="nav-badge">{link.badge}</span>
              )}
            </button>
          ))}
        </div>
        <div className="navbar-user">
          <div className="avatar">{initials}</div>
          <button className="btn-logout" onClick={onLogout}>
            Logout
          </button>
        </div>
      </nav>
    );
  }
  ```

- [ ] Create `frontend/src/components/Navbar.css` with the following content:
  ```css
  .navbar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    background: #fff;
    border-bottom: 1px solid var(--color-border);
    padding: 0 32px;
    height: 60px;
    position: sticky;
    top: 0;
    z-index: 100;
    box-shadow: var(--shadow);
  }
  .navbar-brand {
    font-size: 18px;
    font-weight: 800;
    color: var(--color-primary-dark);
    cursor: pointer;
    letter-spacing: -0.5px;
  }
  .navbar-brand span {
    color: var(--color-primary);
  }
  .navbar-links {
    display: flex;
    gap: 4px;
  }
  .nav-link {
    background: none;
    border: none;
    padding: 6px 14px;
    border-radius: 6px;
    font-size: 14px;
    font-weight: 500;
    color: var(--color-muted);
    cursor: pointer;
    position: relative;
    transition: all 0.15s;
  }
  .nav-link:hover {
    color: var(--color-primary);
    background: var(--color-primary-light);
  }
  .nav-link.active {
    color: var(--color-primary-dark);
    font-weight: 600;
    border-bottom: 2px solid var(--color-primary);
    border-radius: 0;
  }
  .nav-badge {
    position: absolute;
    top: 2px;
    right: 4px;
    background: #ef4444;
    color: #fff;
    font-size: 9px;
    font-weight: 700;
    padding: 1px 5px;
    border-radius: 8px;
  }
  .navbar-user {
    display: flex;
    align-items: center;
    gap: 12px;
  }
  .avatar {
    width: 34px;
    height: 34px;
    border-radius: 50%;
    background: var(--color-primary-light);
    color: var(--color-primary-dark);
    font-weight: 700;
    font-size: 13px;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .btn-logout {
    background: none;
    border: 1px solid var(--color-border);
    border-radius: 6px;
    padding: 5px 12px;
    font-size: 13px;
    color: var(--color-muted);
    cursor: pointer;
  }
  .btn-logout:hover {
    background: var(--color-danger-light);
    color: var(--color-danger);
    border-color: var(--color-danger);
  }
  @media (max-width: 768px) {
    .navbar {
      padding: 0 16px;
    }
    .nav-link {
      padding: 6px 8px;
      font-size: 12px;
    }
  }
  ```
- [ ] Open `frontend/src/App.jsx` and replace the inline `Header` component (or any existing nav/header JSX) with `<Navbar user={user} onLogout={handleLogout} />`.
- [ ] Add the import at the top of `App.jsx`: `import Navbar from './components/Navbar';`

---

## Task 5.3: Redesign auth pages

- [ ] Create `frontend/src/components/AuthCard.css` with the following content:
  ```css
  .auth-page {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--color-bg);
  }
  .auth-card {
    background: #fff;
    border-radius: var(--radius);
    padding: 40px;
    width: 100%;
    max-width: 400px;
    box-shadow: var(--shadow-md);
  }
  .auth-logo {
    font-size: 22px;
    font-weight: 800;
    color: var(--color-primary-dark);
    text-align: center;
    margin-bottom: 8px;
  }
  .auth-logo span {
    color: var(--color-primary);
  }
  .auth-title {
    font-size: 20px;
    font-weight: 700;
    text-align: center;
    margin-bottom: 4px;
  }
  .auth-subtitle {
    color: var(--color-muted);
    font-size: 14px;
    text-align: center;
    margin-bottom: 24px;
  }
  .form-group {
    margin-bottom: 16px;
  }
  .form-group label {
    display: block;
    font-size: 13px;
    font-weight: 600;
    margin-bottom: 6px;
    color: var(--color-text);
  }
  .form-group input,
  .form-group select {
    width: 100%;
    padding: 10px 14px;
    border: 1.5px solid var(--color-border);
    border-radius: 8px;
    font-size: 14px;
    font-family: var(--font);
    transition: border-color 0.15s;
  }
  .form-group input:focus,
  .form-group select:focus {
    outline: none;
    border-color: var(--color-primary);
  }
  .form-error {
    color: var(--color-danger);
    font-size: 13px;
    margin-top: 4px;
  }
  .auth-submit {
    width: 100%;
    margin-top: 8px;
    padding: 12px;
    font-size: 15px;
  }
  .auth-footer {
    text-align: center;
    margin-top: 20px;
    font-size: 13px;
    color: var(--color-muted);
  }
  .auth-footer button {
    background: none;
    border: none;
    color: var(--color-primary);
    font-weight: 600;
    cursor: pointer;
  }
  ```
- [ ] Replace the entire contents of `frontend/src/Login.jsx` with:

  ```jsx
  import React, { useState } from "react";
  import "./components/AuthCard.css";

  export default function Login({
    onLoginSuccess,
    onSwitchToSignUp,
    onSwitchToForgotPassword,
  }) {
    const [email, setEmail] = useState("");
    const [password, setPassword] = useState("");
    const [error, setError] = useState("");
    const [loading, setLoading] = useState(false);

    async function handleSubmit(e) {
      e.preventDefault();
      setError("");
      setLoading(true);
      try {
        const res = await fetch("/api/login", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ email, password }),
        });
        const data = await res.json();
        if (!res.ok) {
          setError(data.message || "Login failed");
          return;
        }
        localStorage.setItem("token", data.token);
        localStorage.setItem("user", JSON.stringify(data.user));
        onLoginSuccess(data.user);
      } catch {
        setError("Network error. Please try again.");
      } finally {
        setLoading(false);
      }
    }

    return (
      <div className="auth-page">
        <div className="auth-card">
          <div className="auth-logo">
            JobMatch<span>AI</span>
          </div>
          <h2 className="auth-title">Welcome back</h2>
          <p className="auth-subtitle">Sign in to your account</p>
          <form onSubmit={handleSubmit}>
            <div className="form-group">
              <label>Email</label>
              <input
                type="email"
                value={email}
                onChange={(e) => setEmail(e.target.value)}
                placeholder="you@example.com"
                required
              />
            </div>
            <div className="form-group">
              <label>Password</label>
              <input
                type="password"
                value={password}
                onChange={(e) => setPassword(e.target.value)}
                placeholder="••••••••"
                required
              />
            </div>
            {error && <p className="form-error">{error}</p>}
            <button
              type="submit"
              className="btn-primary auth-submit"
              disabled={loading}
            >
              {loading ? "Signing in..." : "Sign In"}
            </button>
          </form>
          <div className="auth-footer">
            <button onClick={onSwitchToForgotPassword}>Forgot password?</button>
            &nbsp;·&nbsp;
            <button onClick={onSwitchToSignUp}>Create account</button>
          </div>
        </div>
      </div>
    );
  }
  ```

- [ ] Delete `frontend/src/Login.css` (styles are now in `AuthCard.css`).
- [ ] Update `frontend/src/SignUp.jsx` to:
  - Replace its CSS import with `import './components/AuthCard.css';`
  - Wrap all content in `<div className="auth-page"><div className="auth-card">...</div></div>`
  - Use `.auth-logo`, `.auth-title`, `.auth-subtitle`, `.form-group`, `.form-error`, `.auth-submit`, `.auth-footer` classes throughout
  - Delete `frontend/src/SignUp.css`
- [ ] Update `frontend/src/ForgotPassword.jsx` to:
  - Replace its CSS import with `import './components/AuthCard.css';`
  - Wrap all content in `<div className="auth-page"><div className="auth-card">...</div></div>`
  - Use `.auth-logo`, `.auth-title`, `.auth-subtitle`, `.form-group`, `.form-error`, `.auth-submit`, `.auth-footer` classes throughout
  - Delete `frontend/src/ForgotPassword.css`

---

## Task 5.4: Redesign Profile page

- [ ] Create `frontend/src/pages/Profile.css` with the following content:
  ```css
  .profile-page {
    max-width: 700px;
    margin: 32px auto;
    padding: 0 16px;
  }
  .profile-header-card {
    background: #fff;
    border-radius: var(--radius);
    padding: 28px;
    display: flex;
    align-items: center;
    gap: 20px;
    box-shadow: var(--shadow);
    margin-bottom: 20px;
  }
  .profile-avatar-large {
    width: 64px;
    height: 64px;
    border-radius: 50%;
    background: var(--color-primary-light);
    color: var(--color-primary-dark);
    font-size: 24px;
    font-weight: 700;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .profile-role-badge {
    display: inline-block;
    background: var(--color-primary-light);
    color: var(--color-primary-dark);
    font-size: 12px;
    font-weight: 600;
    padding: 2px 10px;
    border-radius: 10px;
    margin-top: 4px;
  }
  .profile-form-card {
    background: #fff;
    border-radius: var(--radius);
    padding: 28px;
    box-shadow: var(--shadow);
    margin-bottom: 20px;
  }
  .form-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
  }
  .form-group.full-width {
    grid-column: 1 / -1;
  }
  .form-group textarea {
    width: 100%;
    padding: 10px 14px;
    border: 1.5px solid var(--color-border);
    border-radius: 8px;
    font-size: 14px;
    font-family: var(--font);
    resize: vertical;
  }
  .skills-input-row {
    display: flex;
    gap: 8px;
    margin-bottom: 12px;
  }
  .skills-input-row input {
    flex: 1;
    padding: 8px 12px;
    border: 1.5px solid var(--color-border);
    border-radius: 8px;
    font-size: 14px;
    font-family: var(--font);
  }
  .skill-remove {
    background: none;
    border: none;
    color: var(--color-muted);
    cursor: pointer;
    font-size: 14px;
    margin-left: 4px;
  }
  .skill-remove:hover {
    color: var(--color-danger);
  }
  .form-actions {
    display: flex;
    gap: 12px;
    justify-content: flex-end;
    margin-top: 20px;
  }
  ```
- [ ] Open `frontend/src/Profile.jsx` (or `frontend/src/pages/Profile.jsx` if it was moved in Sprint 3/4) and:
  - Replace the existing CSS import with `import './Profile.css';`
  - Add a profile header card at the top showing:
    - `.profile-avatar-large` div containing the user's initials (first letters of name words)
    - User's full name in a heading
    - A `.profile-role-badge` span showing the user's role (e.g. "employee" or "employer")
  - Wrap the edit form in a `.profile-form-card` div
  - Apply `.form-grid` to the two-column section of the form
  - Mark full-width fields (bio, skills textarea) with `.form-group.full-width`
  - Replace the skills text input with a `.skills-input-row` that has an input + "Add" button
  - Render each existing skill as a `.skill-tag` with a `.skill-remove` button (`×`) beside it
  - Place Save/Cancel buttons in a `.form-actions` row using `.btn-primary` and `.btn-outline`
  - Delete the old `frontend/src/Profile.css` if it existed at the root src level

---

## Task 5.5: Redesign Jobs page CSS

- [ ] Create `frontend/src/pages/Jobs.css` with the following content:
  ```css
  .jobs-page {
    padding: 24px 32px;
  }
  .jobs-tabs {
    display: flex;
    gap: 4px;
    margin-bottom: 20px;
    border-bottom: 1px solid var(--color-border);
    padding-bottom: 0;
  }
  .tab-btn {
    background: none;
    border: none;
    padding: 10px 20px;
    font-size: 14px;
    font-weight: 600;
    color: var(--color-muted);
    cursor: pointer;
    border-bottom: 2px solid transparent;
    margin-bottom: -1px;
  }
  .tab-btn.active {
    color: var(--color-primary);
    border-bottom-color: var(--color-primary);
  }
  .jobs-layout {
    display: grid;
    grid-template-columns: 220px 1fr;
    gap: 24px;
  }
  .jobs-sidebar {
    background: #fff;
    border-radius: var(--radius);
    padding: 20px;
    box-shadow: var(--shadow);
    height: fit-content;
    position: sticky;
    top: 80px;
  }
  .jobs-sidebar h3 {
    font-size: 14px;
    font-weight: 700;
    margin-bottom: 16px;
    color: var(--color-text);
  }
  .jobs-sidebar label {
    display: block;
    font-size: 12px;
    font-weight: 600;
    color: var(--color-muted);
    margin-bottom: 12px;
  }
  .jobs-sidebar input[type="text"],
  .jobs-sidebar select {
    width: 100%;
    padding: 7px 10px;
    border: 1.5px solid var(--color-border);
    border-radius: 6px;
    font-size: 13px;
    font-family: var(--font);
    margin-top: 4px;
  }
  .jobs-sidebar input[type="range"] {
    width: 100%;
    margin-top: 4px;
  }
  .jobs-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: 16px;
    align-content: start;
  }
  .job-card {
    background: #fff;
    border-radius: var(--radius);
    padding: 20px;
    box-shadow: var(--shadow);
    cursor: pointer;
    border: 1.5px solid transparent;
    transition: all 0.15s;
  }
  .job-card:hover {
    border-color: var(--color-primary);
    box-shadow: var(--shadow-md);
  }
  .job-card-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 8px;
  }
  .job-card-header h3 {
    font-size: 15px;
    font-weight: 700;
    color: var(--color-text);
  }
  .job-card-header p {
    font-size: 13px;
    color: var(--color-muted);
    margin-top: 2px;
  }
  .job-meta {
    display: flex;
    gap: 8px;
    margin: 8px 0;
  }
  .job-meta span {
    font-size: 12px;
    color: var(--color-muted);
    background: var(--color-bg);
    padding: 2px 8px;
    border-radius: 6px;
  }
  .job-skills {
    display: flex;
    flex-wrap: wrap;
    gap: 4px;
    margin-top: 8px;
  }
  .modal-company {
    color: var(--color-muted);
    margin-bottom: 12px;
  }
  .modal-deadline {
    color: var(--color-muted);
    font-size: 13px;
    margin-bottom: 16px;
  }
  .skill-gap-section {
    background: var(--color-bg);
    border-radius: 8px;
    padding: 16px;
    margin: 16px 0;
  }
  .skill-rows {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
    margin-top: 12px;
  }
  .skill-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 4px;
    margin-top: 6px;
  }
  .match-score-ring {
    text-align: center;
    font-size: 28px;
    font-weight: 800;
    margin-bottom: 8px;
  }
  .modal h4 {
    font-size: 13px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    color: var(--color-muted);
    margin: 16px 0 8px;
  }
  .apply-message {
    padding: 10px 14px;
    border-radius: 8px;
    background: var(--color-primary-light);
    color: var(--color-primary-dark);
    font-size: 13px;
    margin: 12px 0;
  }
  .muted {
    color: var(--color-muted);
    font-size: 12px;
  }
  @media (max-width: 768px) {
    .jobs-layout {
      grid-template-columns: 1fr;
    }
    .jobs-sidebar {
      position: static;
    }
    .skill-rows {
      grid-template-columns: 1fr;
    }
  }
  ```
- [ ] Open `frontend/src/pages/Jobs.jsx` and add at the top: `import './Jobs.css';`
- [ ] Remove or replace any inline `style={{...}}` props that conflict with the new class-based styles.

---

## Task 5.6: Redesign remaining pages

### Dashboard

- [ ] Create `frontend/src/pages/Dashboard.css` with the following content:
  ```css
  .dashboard-page {
    padding: 32px;
    max-width: 900px;
    margin: 0 auto;
  }
  .dashboard-page h2 {
    font-size: 22px;
    font-weight: 800;
    margin-bottom: 24px;
  }
  .stats-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 16px;
    margin-bottom: 28px;
  }
  .stat-card {
    background: #fff;
    border-radius: var(--radius);
    padding: 24px;
    box-shadow: var(--shadow);
    text-align: center;
    border-top: 3px solid var(--color-primary);
  }
  .stat-number {
    font-size: 36px;
    font-weight: 800;
    color: var(--color-primary);
  }
  .stat-label {
    font-size: 13px;
    color: var(--color-muted);
    margin-top: 4px;
    font-weight: 600;
  }
  .dashboard-actions {
    display: flex;
    gap: 12px;
    margin-bottom: 28px;
  }
  .recent-activity {
    background: #fff;
    border-radius: var(--radius);
    padding: 24px;
    box-shadow: var(--shadow);
  }
  .recent-activity h3 {
    font-size: 15px;
    font-weight: 700;
    margin-bottom: 16px;
  }
  .recent-activity ul {
    list-style: none;
  }
  .recent-activity li {
    padding: 10px 0;
    border-bottom: 1px solid var(--color-border);
    font-size: 14px;
    display: flex;
    align-items: center;
    gap: 8px;
    flex-wrap: wrap;
  }
  .recent-activity li:last-child {
    border-bottom: none;
  }
  ```
- [ ] Open `frontend/src/pages/Dashboard.jsx` and add at the top: `import './Dashboard.css';`
- [ ] Apply `.dashboard-page`, `.stats-grid`, `.stat-card`, `.stat-number`, `.stat-label`, `.dashboard-actions`, `.recent-activity` classes to the appropriate JSX elements.

### Applications

- [ ] Create `frontend/src/pages/Applications.css` with the following content:
  ```css
  .applications-page {
    padding: 32px;
    max-width: 900px;
    margin: 0 auto;
  }
  .applications-page h2 {
    font-size: 22px;
    font-weight: 800;
    margin-bottom: 24px;
  }
  .applications-table {
    width: 100%;
    border-collapse: collapse;
    background: #fff;
    border-radius: var(--radius);
    overflow: hidden;
    box-shadow: var(--shadow);
  }
  .applications-table th {
    background: var(--color-bg);
    padding: 12px 16px;
    text-align: left;
    font-size: 12px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    color: var(--color-muted);
    border-bottom: 1px solid var(--color-border);
  }
  .applications-table td {
    padding: 14px 16px;
    border-bottom: 1px solid var(--color-border);
    font-size: 14px;
  }
  .applications-table tr:last-child td {
    border-bottom: none;
  }
  .applications-table tr:hover td {
    background: var(--color-bg);
  }
  ```
- [ ] Open `frontend/src/pages/Applications.jsx` and add at the top: `import './Applications.css';`
- [ ] Replace inline styles on the table and its rows/headers with the `.applications-table` class-based styles.
- [ ] Render the application status in each row using `<span className={`status-chip status-${status.toLowerCase()}`}>{status}</span>`.

### MyJobs

- [ ] Create `frontend/src/pages/MyJobs.css` with the following content:
  ```css
  .my-jobs-page {
    padding: 32px;
    max-width: 1000px;
    margin: 0 auto;
  }
  .page-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 24px;
  }
  .page-header h2 {
    font-size: 22px;
    font-weight: 800;
  }
  .jobs-table {
    width: 100%;
    border-collapse: collapse;
    background: #fff;
    border-radius: var(--radius);
    overflow: hidden;
    box-shadow: var(--shadow);
  }
  .jobs-table th {
    background: var(--color-bg);
    padding: 12px 16px;
    text-align: left;
    font-size: 12px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    color: var(--color-muted);
    border-bottom: 1px solid var(--color-border);
  }
  .jobs-table td {
    padding: 14px 16px;
    border-bottom: 1px solid var(--color-border);
    font-size: 14px;
  }
  .action-cell {
    display: flex;
    gap: 6px;
  }
  .edit-modal {
    max-width: 700px;
  }
  .form-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
  }
  .form-grid .full-width {
    grid-column: 1 / -1;
  }
  .form-grid label {
    font-size: 13px;
    font-weight: 600;
    display: flex;
    flex-direction: column;
    gap: 4px;
  }
  .form-grid input,
  .form-grid textarea,
  .form-grid select {
    padding: 8px 12px;
    border: 1.5px solid var(--color-border);
    border-radius: 8px;
    font-size: 14px;
    font-family: var(--font);
  }
  .modal-actions {
    display: flex;
    gap: 12px;
    justify-content: flex-end;
    margin-top: 24px;
  }
  .confirm-modal {
    max-width: 400px;
    text-align: center;
  }
  .confirm-modal h3 {
    margin-bottom: 12px;
  }
  .confirm-modal p {
    color: var(--color-muted);
    font-size: 14px;
    margin-bottom: 24px;
  }
  ```
- [ ] Open `frontend/src/pages/MyJobs.jsx` and add at the top: `import './MyJobs.css';`
- [ ] Apply `.my-jobs-page`, `.page-header`, `.jobs-table`, `.action-cell`, `.edit-modal`, `.form-grid`, `.modal-actions`, `.confirm-modal` classes to the appropriate JSX elements.
- [ ] Use `.btn-sm` for "Edit" / "View Applicants" buttons and `.btn-danger-sm` for "Delete" buttons in each table row's action cell.

### Applicants

- [ ] Create `frontend/src/pages/Applicants.css` with the following content:
  ```css
  .applicants-page {
    padding: 32px;
    max-width: 1100px;
    margin: 0 auto;
  }
  .filter-bar {
    display: flex;
    gap: 8px;
    margin-bottom: 20px;
  }
  .filter-btn {
    background: #fff;
    border: 1.5px solid var(--color-border);
    border-radius: 20px;
    padding: 6px 16px;
    font-size: 13px;
    font-weight: 600;
    cursor: pointer;
    color: var(--color-muted);
  }
  .filter-btn.active {
    background: var(--color-primary);
    color: #fff;
    border-color: var(--color-primary);
  }
  .applicants-table {
    width: 100%;
    border-collapse: collapse;
    background: #fff;
    border-radius: var(--radius);
    overflow: hidden;
    box-shadow: var(--shadow);
  }
  .applicants-table th {
    background: var(--color-bg);
    padding: 12px 16px;
    text-align: left;
    font-size: 12px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    color: var(--color-muted);
    border-bottom: 1px solid var(--color-border);
  }
  .applicants-table td {
    padding: 12px 16px;
    border-bottom: 1px solid var(--color-border);
    font-size: 13px;
    vertical-align: middle;
  }
  ```
- [ ] Open `frontend/src/pages/Applicants.jsx` and add at the top: `import './Applicants.css';`
- [ ] Apply `.applicants-page`, `.filter-bar`, `.filter-btn` (with `.active` toggled by state), `.applicants-table` classes throughout.
- [ ] Render match scores using `<span className="score-badge" style={{ background: score >= 70 ? 'var(--color-success)' : score >= 40 ? 'var(--color-primary)' : 'var(--color-muted)' }}>{score}%</span>`.

### CreateJobPost

- [ ] Replace the contents of `frontend/src/CreateJobPost.css` with styles matching the Profile form card pattern:
  ```css
  .create-job-page {
    max-width: 700px;
    margin: 32px auto;
    padding: 0 16px;
  }
  .create-job-page h2 {
    font-size: 22px;
    font-weight: 800;
    margin-bottom: 24px;
  }
  .create-job-card {
    background: #fff;
    border-radius: var(--radius);
    padding: 32px;
    box-shadow: var(--shadow);
  }
  .form-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
  }
  .form-group {
    margin-bottom: 0;
  }
  .form-group.full-width {
    grid-column: 1 / -1;
  }
  .form-group label {
    display: block;
    font-size: 13px;
    font-weight: 600;
    margin-bottom: 6px;
    color: var(--color-text);
  }
  .form-group input,
  .form-group select,
  .form-group textarea {
    width: 100%;
    padding: 10px 14px;
    border: 1.5px solid var(--color-border);
    border-radius: 8px;
    font-size: 14px;
    font-family: var(--font);
    transition: border-color 0.15s;
  }
  .form-group input:focus,
  .form-group select:focus,
  .form-group textarea:focus {
    outline: none;
    border-color: var(--color-primary);
  }
  .form-group textarea {
    resize: vertical;
    min-height: 100px;
  }
  .skills-input-row {
    display: flex;
    gap: 8px;
    margin-bottom: 12px;
  }
  .skills-input-row input {
    flex: 1;
    padding: 8px 12px;
    border: 1.5px solid var(--color-border);
    border-radius: 8px;
    font-size: 14px;
    font-family: var(--font);
  }
  .skill-remove {
    background: none;
    border: none;
    color: var(--color-muted);
    cursor: pointer;
    font-size: 14px;
    margin-left: 4px;
  }
  .skill-remove:hover {
    color: var(--color-danger);
  }
  .form-actions {
    display: flex;
    gap: 12px;
    justify-content: flex-end;
    margin-top: 24px;
  }
  ```
- [ ] Open `frontend/src/CreateJobPost.jsx`, ensure it imports `./CreateJobPost.css`, and wrap the form in `.create-job-page > .create-job-card`.
- [ ] Apply `.form-grid` and `.form-group` + `.form-group.full-width` to the form fields.
- [ ] Place the submit button in `.form-actions` using `.btn-primary`.

### Messages (placeholder for Sprint 6)

- [ ] Create `frontend/src/pages/Messages.css` as a placeholder with a two-column chat layout skeleton:
  ```css
  /* Messages layout — full implementation in Sprint 6 */
  .messages-page {
    display: grid;
    grid-template-columns: 280px 1fr;
    height: calc(100vh - 60px);
    background: #fff;
  }
  .messages-sidebar {
    border-right: 1px solid var(--color-border);
    overflow-y: auto;
  }
  .messages-sidebar h3 {
    padding: 20px;
    font-size: 15px;
    font-weight: 700;
    border-bottom: 1px solid var(--color-border);
  }
  .conversation-item {
    padding: 14px 20px;
    cursor: pointer;
    border-bottom: 1px solid var(--color-border);
    transition: background 0.1s;
  }
  .conversation-item:hover {
    background: var(--color-bg);
  }
  .conversation-item.active {
    background: var(--color-primary-light);
  }
  .conv-name {
    font-weight: 600;
    font-size: 14px;
  }
  .conv-preview {
    font-size: 12px;
    color: var(--color-muted);
    margin-top: 2px;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }
  .messages-main {
    display: flex;
    flex-direction: column;
  }
  .messages-header {
    padding: 16px 24px;
    border-bottom: 1px solid var(--color-border);
    font-weight: 700;
    font-size: 15px;
  }
  .messages-body {
    flex: 1;
    overflow-y: auto;
    padding: 24px;
    display: flex;
    flex-direction: column;
    gap: 12px;
  }
  .message-bubble {
    max-width: 60%;
    padding: 10px 14px;
    border-radius: 12px;
    font-size: 14px;
    line-height: 1.5;
  }
  .message-bubble.sent {
    background: var(--color-primary);
    color: #fff;
    align-self: flex-end;
    border-bottom-right-radius: 4px;
  }
  .message-bubble.received {
    background: var(--color-bg);
    color: var(--color-text);
    align-self: flex-start;
    border-bottom-left-radius: 4px;
  }
  .messages-input-row {
    padding: 16px 24px;
    border-top: 1px solid var(--color-border);
    display: flex;
    gap: 8px;
  }
  .messages-input-row input {
    flex: 1;
    padding: 10px 14px;
    border: 1.5px solid var(--color-border);
    border-radius: 8px;
    font-size: 14px;
    font-family: var(--font);
  }
  .messages-input-row input:focus {
    outline: none;
    border-color: var(--color-primary);
  }
  @media (max-width: 768px) {
    .messages-page {
      grid-template-columns: 1fr;
    }
    .messages-sidebar {
      display: none;
    }
  }
  ```
- [ ] Note: `Messages.jsx` implementation is deferred to Sprint 6. This CSS file is created now so the route does not crash if the page is scaffolded.

---

## Task 5.7: Verify full visual consistency

- [ ] Run `cd frontend && npm start` and open `http://localhost:3000` in the browser.
- [ ] Verify the Inter font is loading (check Network tab in DevTools for `fonts.googleapis.com`).
- [ ] Walk through each route and confirm:
  - [ ] `/login` — centered card, indigo "Sign In" button, no leftover old styles
  - [ ] `/signup` — same auth-card layout, consistent form-group inputs
  - [ ] `/forgot-password` — same auth-card layout
  - [ ] `/jobs` — sidebar + card grid, `.job-card:hover` shows indigo border
  - [ ] `/applications` — table with status chips (indigo = pending, green = shortlisted)
  - [ ] `/dashboard` — three stat cards with indigo top-border accent, recent activity list
  - [ ] `/my-jobs` — table with `.btn-sm` / `.btn-danger-sm` action buttons
  - [ ] `/create-job` — form card matching profile layout
  - [ ] `/profile` — header card with avatar initials, role badge, editable form below
- [ ] Open Chrome DevTools, toggle device toolbar to iPhone SE (375px wide), and verify:
  - [ ] Navbar links do not overflow (font shrinks to 12px via media query)
  - [ ] Jobs page switches to single-column layout
  - [ ] Auth cards remain readable and centered
- [ ] Fix any CSS collision (e.g. conflicting `.form-grid` definitions between pages).
- [ ] Commit all changes:
  ```
  git add frontend/public/index.html \
          frontend/src/index.css \
          frontend/src/components/ \
          frontend/src/Login.jsx \
          frontend/src/SignUp.jsx \
          frontend/src/ForgotPassword.jsx \
          frontend/src/CreateJobPost.css \
          frontend/src/pages/
  git commit -m "feat: sprint 5 - complete UI redesign with Style B"
  ```
